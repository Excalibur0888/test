import Header from './Header';
import Statistic from './Statistic';

const Main = () => (
  <section id="main" className="main">
    <Header />
    <Statistic />
  </section>
);

export default Main;
